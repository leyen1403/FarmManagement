@model List<FarmManagement.Application.DTOs.Crops.CropCareTypeDto>

@{
    ViewData["Title"] = "Loại chăm sóc";
    ViewData["Breadcrumb"] = "Loại chăm sóc";
}

@* ==================== Page Header ==================== *@
<div class="page-header d-flex justify-content-between align-items-center mb-4">
    <div>
        <h1 class="page-title">Loại chăm sóc</h1>
        <p class="page-subtitle mb-0">Quản lý các loại chăm sóc cây trồng trong hệ thống</p>
    </div>
    <div class="page-actions">
        <a asp-action="Create" class="btn btn-primary">
            <i class="bi bi-plus-lg me-1"></i> Thêm mới
        </a>
    </div>
</div>

@* ==================== Notifications ==================== *@
@if (TempData["Error"] != null)
{
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <i class="bi bi-exclamation-triangle me-2"></i>
        @TempData["Error"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}

@if (TempData["Success"] != null)
{
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        <i class="bi bi-check-circle me-2"></i>
        @TempData["Success"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}

@* ==================== Main Content ==================== *@
<div class="table-container">
    <div class="table-toolbar">
        <div class="table-search">
            <i class="bi bi-search"></i>
            <input type="text" class="form-control" id="searchInput" placeholder="Tìm kiếm loại chăm sóc...">
        </div>
        <div class="d-flex gap-2">
            <span class="badge bg-secondary">Tổng: @Model.Count</span>
            <span class="badge badge-active">Hoạt động: @Model.Count(x => x.IsActive)</span>
            <span class="badge badge-inactive">Tắt: @Model.Count(x => !x.IsActive)</span>
        </div>
    </div>

    @if (Model.Any())
    {
        <table class="table data-table mb-0" id="dataTable">
            <thead>
                <tr>
                    <th width="60">#</th>
                    <th>Mã</th>
                    <th>Tên loại chăm sóc</th>
                    <th>Mô tả</th>
                    <th width="120">Trạng thái</th>
                    <th width="180">Thao tác</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td class="text-muted">@item.Id</td>
                        <td>
                            <code class="bg-light px-2 py-1 rounded">@item.Code</code>
                        </td>
                        <td>
                            <div class="d-flex align-items-center">
                                <div class="bg-info bg-opacity-10 text-info rounded p-2 me-2">
                                    <i class="bi bi-heart-pulse"></i>
                                </div>
                                <span class="fw-medium">@item.Name</span>
                            </div>
                        </td>
                        <td class="text-muted text-truncate-2" style="max-width: 200px;">
                            @(item.Description ?? "-")
                        </td>
                        <td>
                            @if (item.IsActive)
                            {
                                <span class="badge badge-status badge-active">
                                    <i class="bi bi-check-circle me-1"></i>Hoạt động
                                </span>
                            }
                            else
                            {
                                <span class="badge badge-status badge-inactive">
                                    <i class="bi bi-x-circle me-1"></i>Tắt
                                </span>
                            }
                        </td>
                        <td>
                            <div class="action-buttons">
                                <a asp-action="Edit" asp-route-id="@item.Id"
                                   class="btn btn-sm btn-outline-primary" title="Sửa">
                                    <i class="bi bi-pencil"></i>
                                </a>
                                <form asp-action="ToggleActive" asp-route-id="@item.Id" method="post" class="d-inline">
                                    @Html.AntiForgeryToken()
                                    <button type="submit"
                                            class="btn btn-sm @(item.IsActive ? "btn-outline-warning" : "btn-outline-success")"
                                            title="@(item.IsActive ? "Tắt" : "Bật")">
                                        <i class="bi @(item.IsActive ? "bi-toggle-off" : "bi-toggle-on")"></i>
                                    </button>
                                </form>
                                <button type="button" class="btn btn-sm btn-outline-danger"
                                        title="Xóa"
                                        onclick="confirmDelete(@item.Id, '@item.Name')">
                                    <i class="bi bi-trash"></i>
                                </button>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <div class="empty-state">
            <div class="empty-state-icon">
                <i class="bi bi-heart-pulse"></i>
            </div>
            <div class="empty-state-title">Chưa có loại chăm sóc</div>
            <p class="text-muted">Bắt đầu bằng cách thêm loại chăm sóc đầu tiên vào hệ thống.</p>
            <a asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-lg me-1"></i> Thêm mới
            </a>
        </div>
    }
</div>

@* ==================== Modals & Hidden Forms ==================== *@
<form id="deleteForm" asp-action="Delete" method="post" style="display: none;">
    @Html.AntiForgeryToken()
    <input type="hidden" name="id" id="deleteId" />
</form>

<div class="modal fade" id="deleteModal" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">
                    <i class="bi bi-exclamation-triangle text-danger me-2"></i>Xác nhận xóa
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <p>Bạn có chắc chắn muốn xóa loại chăm sóc <strong id="deleteItemName"></strong>?</p>
                <p class="text-muted small mb-0">Hành động này không thể hoàn tác và có thể ảnh hưởng đến lịch sử chăm sóc.</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Hủy</button>
                <button type="button" class="btn btn-danger" onclick="submitDelete()">
                    <i class="bi bi-trash me-1"></i>Xóa ngay
                </button>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        // Xử lý tìm kiếm nhanh tại Client
        document.getElementById('searchInput').addEventListener('keyup', function () {
            const filter = this.value.toLowerCase();
            const rows = document.querySelectorAll('#dataTable tbody tr');
            rows.forEach(row => {
                const text = row.textContent.toLowerCase();
                row.style.display = text.includes(filter) ? '' : 'none';
            });
        });

        // Xử lý Modal xóa
        function confirmDelete(id, name) {
            document.getElementById('deleteItemName').textContent = name;
            document.getElementById('deleteId').value = id;
            new bootstrap.Modal(document.getElementById('deleteModal')).show();
        }

        function submitDelete() {
            document.getElementById('deleteForm').submit();
        }
    </script>
}